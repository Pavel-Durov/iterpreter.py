let fibo = fn(x) { 
  if (x < 2) {
    return x;
  }
  return fibo(x-1) + fibo(x-2);
}; 

let result = fibo(7);

puts("result: ", result);



let fibo = fn(x) { if (x < 2) { return x; } return x + 2; }; 